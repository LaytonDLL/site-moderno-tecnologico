// Wait until the page is fully loaded
window.addEventListener('load', () => {
    // Add the 'loaded' class to the body to hide the loader and show the content
    document.body.classList.add('loaded');
});